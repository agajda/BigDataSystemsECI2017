The program is a simple application, like the WordCount, that counts the number 
of occurrences of each card suit and number in a given input set.
It reads a file named cards.txt, with a list of card suits with numbers,
and output the quantities of each of them. 